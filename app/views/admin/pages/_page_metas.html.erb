<table>
  <thead>
    <tr>
      <td>Nombre</td><td>Valor</td><td></td>
    </tr>
  </thead>
  <tbody>
    <% for meta in @page.metas %>
      <tr><td><%= meta.name %></td><td><%= meta.value %></td>
        <td><%= link_to(image_tag('page_white_delete.png'), [:admin, @page, meta], :confirm => 'seguro?',
            :method => :delete, :class => 'action')  %>
        </td>
      </tr>
    <%  end %>
  </tbody>
</table>