<ul>
  <% for page in pages %>
    <li class="<%= page.locked? ? 'area' : 'item'  %>"><%= link_to_if(!page.locked?, page.title, {:action => :pagina, :id => page}) %></li>
    <%= render(:partial => 'menu_items', :locals => {:pages => page.children}) if page.children.size > 0  %>
  <% end %>
</ul>
