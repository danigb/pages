defaults: &defaults
  adapter: mysql
  encoding: utf8
  username: root
  password: db4root
  socket: /var/run/mysqld/mysqld.sock

development:
  database: calcpages_development
  <<: *defaults

test:
  database: calcpages_test 
  <<: *defaults

production:
  database: calcpages_production
  <<: *defaults
